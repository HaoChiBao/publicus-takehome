# Minimal deps for the FastAPI backend (Railway / Render API deploy).
# The full pipeline uses requirements.txt — run that locally or in a separate job.
fastapi==0.111.0
uvicorn[standard]==0.30.1
asyncpg==0.29.0
python-dotenv==1.0.1
