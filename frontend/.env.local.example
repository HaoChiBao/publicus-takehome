# Leave unset in local dev to use Next.js /api proxy (no CORS issues).
# Production (Vercel): no trailing slash — e.g. https://your-api.up.railway.app
# NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# Supabase (only needed if you choose to talk to Supabase directly from the
# client; the app talks to the FastAPI backend by default).
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxx
